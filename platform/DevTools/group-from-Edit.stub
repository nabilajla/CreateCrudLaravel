  <div class="form-group">
    <label for="{{name}}">{{name}}</label>
    <input type="text" name="{{name}}" class="form-control" value="{{${{Model}}->{{name}}}}" id="{{name}}" placeholder="Enter a {{name}}">
  </div>
